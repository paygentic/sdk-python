# UpdateApprovalRequestDecision

approved/rejected: reviewer decision (maker-checker enforced). cancelled: recall a pending approval or reopen an approved one.

## Example Usage

```python
from paygentic_sdk.models import UpdateApprovalRequestDecision
value: UpdateApprovalRequestDecision = "approved"
```


## Values

- `"approved"`
- `"rejected"`
- `"cancelled"`
