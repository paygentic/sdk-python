# OrderType

## Example Usage

```python
from paygentic_sdk.models import OrderType

# Open enum: unrecognized values are captured as UnrecognizedStr
value: OrderType = "new_business"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"new_business"`
- `"renewal"`
- `"expansion"`
