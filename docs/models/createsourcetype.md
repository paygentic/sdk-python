# CreateSourceType

The type of source. Currently only 'stripe_revenue' is supported.

## Example Usage

```python
from paygentic_sdk.models import CreateSourceType
value: CreateSourceType = "stripe_revenue"
```


## Values

- `"stripe_revenue"`
