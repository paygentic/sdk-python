# SourceEventStatus

## Example Usage

```python
from paygentic_sdk.models import SourceEventStatus

# Open enum: unrecognized values are captured as UnrecognizedStr
value: SourceEventStatus = "pending"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"pending"`
- `"processed"`
- `"failed"`
- `"rejected"`
