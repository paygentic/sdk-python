# CreateCostType

The cost type. Only `metered` costs are supported; they track usage via events.

## Example Usage

```python
from paygentic_sdk.models import CreateCostType
value: CreateCostType = "metered"
```


## Values

- `"metered"`
