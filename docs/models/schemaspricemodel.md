# SchemasPriceModel

## Example Usage

```python
from paygentic_sdk.models import SchemasPriceModel

# Open enum: unrecognized values are captured as UnrecognizedStr
value: SchemasPriceModel = "standard"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"standard"`
- `"dynamic"`
- `"volume"`
- `"percentage"`
