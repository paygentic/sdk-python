# PlanVersionPriceSlotModel

## Example Usage

```python
from paygentic_sdk.models import PlanVersionPriceSlotModel

# Open enum: unrecognized values are captured as UnrecognizedStr
value: PlanVersionPriceSlotModel = "standard"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"standard"`
- `"dynamic"`
- `"volume"`
- `"percentage"`
