"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from paygentic_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
import pydantic
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


CreateSubscriptionAdjustmentRequestType = Literal["percentageDiscount",]
r"""The kind of adjustment. `percentageDiscount` reduces every discountable charge by a rate."""


class CreateSubscriptionAdjustmentRequestTypedDict(TypedDict):
    type: CreateSubscriptionAdjustmentRequestType
    r"""The kind of adjustment. `percentageDiscount` reduces every discountable charge by a rate."""
    percentage_discount: str
    r"""The discount rate as a decimal fraction between 0 and 1, sent as a string. \"0.35\" means 35 percent. \"1\" means 100 percent, not 1 percent. At most 6 decimal places. A value of 0 or above 1 is rejected."""
    effective_from: datetime
    r"""The first instant the discount applies. Inclusive."""
    effective_to: NotRequired[Nullable[datetime]]
    r"""The instant the discount stops applying. Exclusive, so a window ending on the same date another begins neither overlaps nor leaves a gap. Null means the discount never stops, and it cannot be ended later — set an instant whenever the deal has a known end date. Must be after effectiveFrom."""
    description: NotRequired[Nullable[str]]
    r"""The deal's own name, shown on each discount line of the invoice."""
    idempotency_key: NotRequired[str]
    r"""A key of your choosing that makes a retry safe. Sending the same key against the same subscription returns the adjustment already created and creates no second one. Without a key a retried request creates a second adjustment, and two percentage discounts compound — two of 0.35 bill 57.75 percent off, not 35 percent."""


class CreateSubscriptionAdjustmentRequest(BaseModel):
    type: CreateSubscriptionAdjustmentRequestType
    r"""The kind of adjustment. `percentageDiscount` reduces every discountable charge by a rate."""

    percentage_discount: Annotated[str, pydantic.Field(alias="percentageDiscount")]
    r"""The discount rate as a decimal fraction between 0 and 1, sent as a string. \"0.35\" means 35 percent. \"1\" means 100 percent, not 1 percent. At most 6 decimal places. A value of 0 or above 1 is rejected."""

    effective_from: Annotated[datetime, pydantic.Field(alias="effectiveFrom")]
    r"""The first instant the discount applies. Inclusive."""

    effective_to: Annotated[
        OptionalNullable[datetime], pydantic.Field(alias="effectiveTo")
    ] = UNSET
    r"""The instant the discount stops applying. Exclusive, so a window ending on the same date another begins neither overlaps nor leaves a gap. Null means the discount never stops, and it cannot be ended later — set an instant whenever the deal has a known end date. Must be after effectiveFrom."""

    description: OptionalNullable[str] = UNSET
    r"""The deal's own name, shown on each discount line of the invoice."""

    idempotency_key: Annotated[
        Optional[str], pydantic.Field(alias="idempotencyKey")
    ] = None
    r"""A key of your choosing that makes a retry safe. Sending the same key against the same subscription returns the adjustment already created and creates no second one. Without a key a retried request creates a second adjustment, and two percentage discounts compound — two of 0.35 bill 57.75 percent off, not 35 percent."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["effectiveTo", "description", "idempotencyKey"])
        nullable_fields = set(["effectiveTo", "description"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    CreateSubscriptionAdjustmentRequest.model_rebuild()
except NameError:
    pass
