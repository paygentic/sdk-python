"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from paygentic_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Dict, Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


CreateCostType = Literal["metered",]
r"""The cost type. Only `metered` costs are supported; they track usage via events."""


CreateCostAggregation = Literal[
    "SUM",
    "COUNT",
    "AVG",
    "MIN",
    "MAX",
    "UNIQUE_COUNT",
    "LATEST",
]
r"""Aggregation method for the metered event."""


class CreateCostRequestTypedDict(TypedDict):
    type: CreateCostType
    r"""The cost type. Only `metered` costs are supported; they track usage via events."""
    name: str
    r"""Human-readable name for the cost."""
    unit_cost: float
    r"""Cost per unit, multiplied by measured quantity to compute total cost. Must be non-negative."""
    currency: str
    r"""ISO 4217 currency code (e.g. USD, EUR)."""
    product_id: str
    r"""Unique identifier for a product"""
    aggregation: CreateCostAggregation
    r"""Aggregation method for the metered event."""
    event_type: str
    r"""CloudEvents type that identifies the metered event."""
    unit: NotRequired[str]
    r"""Unit label for metered costs (e.g. 'token', 'request'). Only valid for metered costs."""
    value_property: NotRequired[str]
    r"""JSONPath to extract a numeric value from event data. Must start with `$.` (example: `$.amount` or `$.payload.bytes`). Required for SUM/AVG/MIN/MAX/LATEST aggregations."""
    group_by: NotRequired[Dict[str, str]]
    r"""Map of dimension name to JSONPath for group-by queries. Each value must start with `$.` (example: `$.region`). Only valid for metered costs."""
    merchant_id: NotRequired[str]
    r"""Unique identifier for an organization"""


class CreateCostRequest(BaseModel):
    type: CreateCostType
    r"""The cost type. Only `metered` costs are supported; they track usage via events."""

    name: str
    r"""Human-readable name for the cost."""

    unit_cost: Annotated[float, pydantic.Field(alias="unitCost")]
    r"""Cost per unit, multiplied by measured quantity to compute total cost. Must be non-negative."""

    currency: str
    r"""ISO 4217 currency code (e.g. USD, EUR)."""

    product_id: Annotated[str, pydantic.Field(alias="productId")]
    r"""Unique identifier for a product"""

    aggregation: CreateCostAggregation
    r"""Aggregation method for the metered event."""

    event_type: Annotated[str, pydantic.Field(alias="eventType")]
    r"""CloudEvents type that identifies the metered event."""

    unit: Optional[str] = None
    r"""Unit label for metered costs (e.g. 'token', 'request'). Only valid for metered costs."""

    value_property: Annotated[Optional[str], pydantic.Field(alias="valueProperty")] = (
        None
    )
    r"""JSONPath to extract a numeric value from event data. Must start with `$.` (example: `$.amount` or `$.payload.bytes`). Required for SUM/AVG/MIN/MAX/LATEST aggregations."""

    group_by: Annotated[Optional[Dict[str, str]], pydantic.Field(alias="groupBy")] = (
        None
    )
    r"""Map of dimension name to JSONPath for group-by queries. Each value must start with `$.` (example: `$.region`). Only valid for metered costs."""

    merchant_id: Annotated[Optional[str], pydantic.Field(alias="merchantId")] = None
    r"""Unique identifier for an organization"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["unit", "valueProperty", "groupBy", "merchantId"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    CreateCostRequest.model_rebuild()
except NameError:
    pass
