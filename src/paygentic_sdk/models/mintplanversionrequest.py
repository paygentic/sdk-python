"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from paygentic_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ReplacePriceTypedDict(TypedDict):
    replaces_price_id: str
    r"""Unique identifier for a price"""
    with_price_id: str
    r"""Unique identifier for a price"""


class ReplacePrice(BaseModel):
    replaces_price_id: Annotated[str, pydantic.Field(alias="replacesPriceId")]
    r"""Unique identifier for a price"""

    with_price_id: Annotated[str, pydantic.Field(alias="withPriceId")]
    r"""Unique identifier for a price"""


class MintPlanVersionRequestTypedDict(TypedDict):
    r"""A reference-by-id price diff applied to the prices of the plan's current version. Every id references an existing price created via POST /prices; inline price definitions are not accepted. An empty body copies the current prices into the new version unchanged. A version must carry at least one price, so a diff that would leave none is rejected."""

    add_prices: NotRequired[List[str]]
    r"""Prices to add to the version. Each must not already be on the plan's current version."""
    remove_prices: NotRequired[List[str]]
    r"""Prices to remove. Each must be on the plan's current version."""
    replace_prices: NotRequired[List[ReplacePriceTypedDict]]
    r"""Prices to swap in place, preserving the slot's lineage so the price keeps its identity where the plan is configured for stable price ids. replacesPriceId must be on the plan's current version; withPriceId is the new price."""


class MintPlanVersionRequest(BaseModel):
    r"""A reference-by-id price diff applied to the prices of the plan's current version. Every id references an existing price created via POST /prices; inline price definitions are not accepted. An empty body copies the current prices into the new version unchanged. A version must carry at least one price, so a diff that would leave none is rejected."""

    add_prices: Annotated[Optional[List[str]], pydantic.Field(alias="addPrices")] = None
    r"""Prices to add to the version. Each must not already be on the plan's current version."""

    remove_prices: Annotated[
        Optional[List[str]], pydantic.Field(alias="removePrices")
    ] = None
    r"""Prices to remove. Each must be on the plan's current version."""

    replace_prices: Annotated[
        Optional[List[ReplacePrice]], pydantic.Field(alias="replacePrices")
    ] = None
    r"""Prices to swap in place, preserving the slot's lineage so the price keeps its identity where the plan is configured for stable price ids. replacesPriceId must be on the plan's current version; withPriceId is the new price."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["addPrices", "removePrices", "replacePrices"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    ReplacePrice.model_rebuild()
except NameError:
    pass
try:
    MintPlanVersionRequest.model_rebuild()
except NameError:
    pass
