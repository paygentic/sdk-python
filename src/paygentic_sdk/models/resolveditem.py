"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .externalreference import ExternalReference, ExternalReferenceTypedDict
from datetime import datetime
from paygentic_sdk.types import BaseModel, Nullable, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List
from typing_extensions import Annotated, TypedDict


class ResolvedItemTypedDict(TypedDict):
    r"""An item a line was tagged with, together with its external accounting/tax codes. Deliberately narrower than the /v0/items Item resource: it carries what is needed to translate a line into a GL/SKU code, not the full record."""

    id: str
    r"""Unique identifier for an item"""
    name: str
    r"""Human-readable item name"""
    catalog_id: Nullable[str]
    r"""The product this item belongs to, for grouping lines by product"""
    archived_at: Nullable[datetime]
    r"""When this item was retired from the catalog. Archived items still resolve here — invoices that already reference them have to keep resolving — but cannot be tagged onto new pricing. Null while active."""
    external_references: List[ExternalReferenceTypedDict]
    r"""The item's external codes, narrowed to the requested provider when one is given. An empty list means the item is not yet mapped for that provider — an actionable state, not an error. More than one may be returned for a single provider, and more than one of those may carry `isPrimary: true`: the uniqueness constraint runs the other way round (an external id is the primary reference of at most one item per merchant), so it does not reduce an item's references to one per provider. Entries are ordered newest first, tie-broken by id, so the order is stable across reads — but a consumer that needs exactly one code must choose deliberately rather than assume there is only one."""


class ResolvedItem(BaseModel):
    r"""An item a line was tagged with, together with its external accounting/tax codes. Deliberately narrower than the /v0/items Item resource: it carries what is needed to translate a line into a GL/SKU code, not the full record."""

    id: str
    r"""Unique identifier for an item"""

    name: str
    r"""Human-readable item name"""

    catalog_id: Annotated[Nullable[str], pydantic.Field(alias="catalogId")]
    r"""The product this item belongs to, for grouping lines by product"""

    archived_at: Annotated[Nullable[datetime], pydantic.Field(alias="archivedAt")]
    r"""When this item was retired from the catalog. Archived items still resolve here — invoices that already reference them have to keep resolving — but cannot be tagged onto new pricing. Null while active."""

    external_references: Annotated[
        List[ExternalReference], pydantic.Field(alias="externalReferences")
    ]
    r"""The item's external codes, narrowed to the requested provider when one is given. An empty list means the item is not yet mapped for that provider — an actionable state, not an error. More than one may be returned for a single provider, and more than one of those may carry `isPrimary: true`: the uniqueness constraint runs the other way round (an external id is the primary reference of at most one item per merchant), so it does not reduce an item's references to one per provider. Entries are ordered newest first, tie-broken by id, so the order is stable across reads — but a consumer that needs exactly one code must choose deliberately rather than assume there is only one."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                m[k] = val

        return m


try:
    ResolvedItem.model_rebuild()
except NameError:
    pass
