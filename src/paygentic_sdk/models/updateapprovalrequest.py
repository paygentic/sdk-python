"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from paygentic_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
import pydantic
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


UpdateApprovalRequestDecision = Literal[
    "approved",
    "rejected",
    "cancelled",
]
r"""approved/rejected: reviewer decision (maker-checker enforced). cancelled: recall a pending approval or reopen an approved one."""


class UpdateApprovalRequestTypedDict(TypedDict):
    decision: UpdateApprovalRequestDecision
    r"""approved/rejected: reviewer decision (maker-checker enforced). cancelled: recall a pending approval or reopen an approved one."""
    note: NotRequired[Nullable[str]]
    actor_id: NotRequired[str]
    r"""Optional. The real user id performing the action. Used when the request is made via a platform-level key on behalf of a human user. Ignored when the caller is a non-platform API key."""


class UpdateApprovalRequest(BaseModel):
    decision: UpdateApprovalRequestDecision
    r"""approved/rejected: reviewer decision (maker-checker enforced). cancelled: recall a pending approval or reopen an approved one."""

    note: OptionalNullable[str] = UNSET

    actor_id: Annotated[Optional[str], pydantic.Field(alias="actorId")] = None
    r"""Optional. The real user id performing the action. Used when the request is made via a platform-level key on behalf of a human user. Ignored when the caller is a non-platform API key."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["note", "actorId"])
        nullable_fields = set(["note"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    UpdateApprovalRequest.model_rebuild()
except NameError:
    pass
