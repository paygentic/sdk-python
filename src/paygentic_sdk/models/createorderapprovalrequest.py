"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from paygentic_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class CreateOrderApprovalRequestTypedDict(TypedDict):
    requester: NotRequired[str]
    r"""Optional. The maker submitting the order. Derived from the authenticated principal; only a platform key may set it explicitly."""
    note: NotRequired[Nullable[str]]


class CreateOrderApprovalRequest(BaseModel):
    requester: Optional[str] = None
    r"""Optional. The maker submitting the order. Derived from the authenticated principal; only a platform key may set it explicitly."""

    note: OptionalNullable[str] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["requester", "note"])
        nullable_fields = set(["note"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
