"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from paygentic_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
import pydantic
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


CreateApprovalRequestResourceType = Literal[
    "order",
    "invoice",
]


CreateApprovalRequestKind = Literal[
    "data_review",
    "financial_review",
    "push",
]
r"""Defaults to data_review."""


class CreateApprovalRequestTypedDict(TypedDict):
    merchant_id: str
    r"""The merchant that owns the resource being approved."""
    resource_type: CreateApprovalRequestResourceType
    resource_id: str
    data_snapshot_hash: str
    r"""A precomputed hash of the resource state being approved. The resource-owning domain builds this snapshot; approvals stays decoupled from any specific resource shape."""
    kind: NotRequired[CreateApprovalRequestKind]
    r"""Defaults to data_review."""
    note: NotRequired[Nullable[str]]
    actor_id: NotRequired[str]
    r"""Optional. The real user id to record as the requester. Used when the request is made via a platform-level key on behalf of a human user (e.g. from the platform Next.js frontend). When supplied and the authenticated principal is 'platform', this value overrides the derived principal as the requester. Ignored when the caller is a non-platform API key."""


class CreateApprovalRequest(BaseModel):
    merchant_id: Annotated[str, pydantic.Field(alias="merchantId")]
    r"""The merchant that owns the resource being approved."""

    resource_type: Annotated[
        CreateApprovalRequestResourceType, pydantic.Field(alias="resourceType")
    ]

    resource_id: Annotated[str, pydantic.Field(alias="resourceId")]

    data_snapshot_hash: Annotated[str, pydantic.Field(alias="dataSnapshotHash")]
    r"""A precomputed hash of the resource state being approved. The resource-owning domain builds this snapshot; approvals stays decoupled from any specific resource shape."""

    kind: Optional[CreateApprovalRequestKind] = None
    r"""Defaults to data_review."""

    note: OptionalNullable[str] = UNSET

    actor_id: Annotated[Optional[str], pydantic.Field(alias="actorId")] = None
    r"""Optional. The real user id to record as the requester. Used when the request is made via a platform-level key on behalf of a human user (e.g. from the platform Next.js frontend). When supplied and the authenticated principal is 'platform', this value overrides the derived principal as the requester. Ignored when the caller is a non-platform API key."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["kind", "note", "actorId"])
        nullable_fields = set(["note"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    CreateApprovalRequest.model_rebuild()
except NameError:
    pass
