"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .mappingcapabilities import MappingCapabilities, MappingCapabilitiesTypedDict
from .merchantintegrationprovider import MerchantIntegrationProvider
from .merchantintegrationstatus import MerchantIntegrationStatus
from datetime import datetime
from paygentic_sdk.types import BaseModel, Nullable, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Any, Dict, Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


MerchantIntegrationObject = Literal["merchantIntegration",]


class MerchantIntegrationTypedDict(TypedDict):
    r"""A merchant's tenant-level connection to an external provider via Ampersand."""

    id: str
    r"""Unique identifier for a merchant integration"""
    merchant_id: str
    r"""Unique identifier for an organization"""
    provider: MerchantIntegrationProvider
    r"""External provider a merchant can connect at the tenant level. `netsuite` and `accountsiq` are returned on reads wherever a connection exists, but connecting them is accepted only in local and development environments; elsewhere the connect request is refused with 404."""
    capabilities: MappingCapabilitiesTypedDict
    r"""What this provider does with an item's external codes.

    Two independent capabilities, not one direction: the behaviours are not alternatives. An integration can resolve an incoming item code *and* be sent a selected one on a different path, so a single `inbound`/`outbound` value would have to misdescribe it or forbid one of its operations.

    A capability that is not declared is unavailable rather than inferred. Declaring one does not make a provider connectable — that still requires its credentials and connection lifecycle.
    """
    external_id: Nullable[str]
    r"""Ampersand installation id."""
    status: MerchantIntegrationStatus
    r"""Connection lifecycle state. Live Ampersand health is separate and not stored here."""
    config: Dict[str, Any]
    metadata: Dict[str, Any]
    connected_at: datetime
    disconnected_at: Nullable[datetime]
    created_at: datetime
    updated_at: datetime
    object: NotRequired[MerchantIntegrationObject]


class MerchantIntegration(BaseModel):
    r"""A merchant's tenant-level connection to an external provider via Ampersand."""

    id: str
    r"""Unique identifier for a merchant integration"""

    merchant_id: Annotated[str, pydantic.Field(alias="merchantId")]
    r"""Unique identifier for an organization"""

    provider: MerchantIntegrationProvider
    r"""External provider a merchant can connect at the tenant level. `netsuite` and `accountsiq` are returned on reads wherever a connection exists, but connecting them is accepted only in local and development environments; elsewhere the connect request is refused with 404."""

    capabilities: MappingCapabilities
    r"""What this provider does with an item's external codes.

    Two independent capabilities, not one direction: the behaviours are not alternatives. An integration can resolve an incoming item code *and* be sent a selected one on a different path, so a single `inbound`/`outbound` value would have to misdescribe it or forbid one of its operations.

    A capability that is not declared is unavailable rather than inferred. Declaring one does not make a provider connectable — that still requires its credentials and connection lifecycle.
    """

    external_id: Annotated[Nullable[str], pydantic.Field(alias="externalId")]
    r"""Ampersand installation id."""

    status: MerchantIntegrationStatus
    r"""Connection lifecycle state. Live Ampersand health is separate and not stored here."""

    config: Dict[str, Any]

    metadata: Dict[str, Any]

    connected_at: Annotated[datetime, pydantic.Field(alias="connectedAt")]

    disconnected_at: Annotated[
        Nullable[datetime], pydantic.Field(alias="disconnectedAt")
    ]

    created_at: Annotated[datetime, pydantic.Field(alias="createdAt")]

    updated_at: Annotated[datetime, pydantic.Field(alias="updatedAt")]

    object: Optional[MerchantIntegrationObject] = "merchantIntegration"

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["object"])
        nullable_fields = set(["externalId", "disconnectedAt"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    MerchantIntegration.model_rebuild()
except NameError:
    pass
