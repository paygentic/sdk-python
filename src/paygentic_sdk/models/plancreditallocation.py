"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from paygentic_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class PlanCreditAllocationTypedDict(TypedDict):
    pricing_unit_id: str
    r"""Unique identifier for a pricing unit"""
    amount: float
    r"""Number of credits minted into the customer's pool for this pricing unit each time the allocation is provisioned. Must be a positive number."""
    recurrence_period: NotRequired[str]
    r"""ISO 8601 duration (e.g. 'P1M') controlling how often the allocation's credits are re-minted on the customer's pool. Recurrence is opt-in: when omitted, the allocation is minted once at subscription activation and never refills — even on a recurring plan. Set this field explicitly for credits that should replenish each period."""


class PlanCreditAllocation(BaseModel):
    pricing_unit_id: Annotated[str, pydantic.Field(alias="pricingUnitId")]
    r"""Unique identifier for a pricing unit"""

    amount: float
    r"""Number of credits minted into the customer's pool for this pricing unit each time the allocation is provisioned. Must be a positive number."""

    recurrence_period: Annotated[
        Optional[str], pydantic.Field(alias="recurrencePeriod")
    ] = None
    r"""ISO 8601 duration (e.g. 'P1M') controlling how often the allocation's credits are re-minted on the customer's pool. Recurrence is opt-in: when omitted, the allocation is minted once at subscription activation and never refills — even on a recurring plan. Set this field explicitly for credits that should replenish each period."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["recurrencePeriod"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    PlanCreditAllocation.model_rebuild()
except NameError:
    pass
